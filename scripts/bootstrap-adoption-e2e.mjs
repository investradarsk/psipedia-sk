import { spawnSync } from "node:child_process";
const now=new Date().toISOString().replaceAll("'","''");
const sql=`DELETE FROM adoption_dogs WHERE slug='e2e-testovaci-rex'; INSERT INTO adoption_dogs (name,slug,status,sex,approximate_age_months,size,weight_kg,breed_mix,color,region,district,city,organization_name,short_description,description,temperament,activity_level,suitable_for_children,suitable_for_dogs,suitable_for_cats,vaccination_status,chipped,neutered,health_notes,adoption_requirements,contact_name,contact_email,published_at,last_verified_at,created_at,updated_at,created_by,updated_by) VALUES ('Testovací Rex','e2e-testovaci-rex','ACTIVE','MALE',30,'MEDIUM',22,1,'čierna','Nitriansky kraj','Nitra','Nitra','E2E útulok','Pokojný testovací pes pripravený na overenie adopčného katalógu.','Tento profil existuje iba v lokálnej CI databáze a nikdy sa neimportuje do produkcie. Slúži na test detailu, filtrov a prístupnosti.','priateľský','MEDIUM','YES','YES','UNKNOWN','COMPLETE','YES','YES','Bez známych obmedzení.','Stabilný a zodpovedný domov.','E2E kontakt','e2e@example.invalid','${now}','${now}','${now}','${now}','ci','ci');`;
const result=spawnSync('npx',['wrangler','d1','execute','DB','--local','--config','dist/server/wrangler.json','--persist-to','.wrangler/state','--command',sql],{stdio:'inherit',shell:process.platform==='win32'});if(result.status!==0)process.exit(result.status??1);
