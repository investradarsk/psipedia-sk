import {requirePartnerAdminMutation} from "@/lib/partner-admin-api";import {PartnerCommercialAdminError,updatePartnerCommercialInterestAdmin} from "@/lib/partner-commercial-admin";
export const dynamic="force-dynamic";export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const auth=await requirePartnerAdminMutation(request);if(auth.response)return auth.response;try{const body=await request.json() as Record<string,unknown>,{id}=await params;return Response.json({lead:await updatePartnerCommercialInterestAdmin({id,status:body.status,adminNote:body.adminNote,adminEmail:auth.user.email})});}catch(error){return Response.json({error:error instanceof Error?error.message:"Zmena zlyhala."},{status:error instanceof PartnerCommercialAdminError?error.status:400})}}
