import {PartnerShell} from "@/components/partner-shell";import {requirePartnerPageIdentity} from "@/lib/partner-page-auth";export const dynamic="force-dynamic";export default async function Page(){await requirePartnerPageIdentity();return <PartnerShell title="Propagácia" description="Možnosti zviditeľnenia na Psipedii pripravujeme."><div className="partner-resource-grid">{["Premium profil","Propagovaný profil","Reklamná kampaň"].map(x=><article className="partner-resource-card" key={x}><span>Pripravujeme</span><h2>{x}</h2><p>Táto možnosť zatiaľ nie je aktívna.</p></article>)}</div></PartnerShell>}
